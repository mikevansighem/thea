# pyup.io config file
# see https://pyup.io/docs/configuration/ for all available options

label_prs: dependancy
schedule: "every week"
pin: True
close_prs: True
search: True

assignees:
  - mikevansighem

requirements:
  - pyproject.toml:
      update: all

pr_prefix: ":arrow_up: "
